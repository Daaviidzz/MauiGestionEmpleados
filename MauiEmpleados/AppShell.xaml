<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiEmpleados.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MauiEmpleados.Views"
    Title="Directorio Corporativo"
    FlyoutBehavior="Flyout"
>


    <FlyoutItem Title="🕴Empleados">
        <ShellContent 
        Title="Lista de Empleados"
        ContentTemplate="{DataTemplate views:EmpleadosPage}"
        Route="EmpleadosPage" />
    </FlyoutItem>

    <FlyoutItem Title="🏦 Departamentos">
        <ShellContent 
        Title="Departamentos"
        ContentTemplate="{DataTemplate views:DepartamentosPage}"
        Route="DepartamentosPage" />
    </FlyoutItem>

    <FlyoutItem Title="🚩 Sedes">
        <ShellContent 
     Title="Sedes"
     ContentTemplate="{DataTemplate views:SedesPage}"
     Route="SedesPage" />
    </FlyoutItem>

    <FlyoutItem Title="⚙️ Ajustes">
        <ShellContent 
        Title="Ajustes"
        ContentTemplate="{DataTemplate views:SettingsPage}"
        Route="SettingsPage" />
    </FlyoutItem>

    <FlyoutItem Title="📊 Estadísticas">
        <ShellContent 
    Title="Estadísticas"
    ContentTemplate="{DataTemplate views:EstadisticasPage}"
    Route="EstadisticasPage" />
    </FlyoutItem>

</Shell>